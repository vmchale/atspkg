module Distribution.ATS ( -- * Cabal helper functions
                          cabalHooks
                        , atsPolyglotBuild
                        ) where

import           Distribution.ATS.Build
